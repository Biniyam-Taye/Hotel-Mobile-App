# Manager Dashboard Environment
# Copy this file to .env and fill in your values

# Backend API URL (default works if backend runs on port 5000)
VITE_API_URL=http://localhost:5000/api/v1

# Admin JWT token — see README below for how to get this
VITE_ADMIN_TOKEN=
